import 'package:flutter/material.dart';

class AppShadows {
  AppShadows._();

  static const card = [
    BoxShadow(color: Color(0x120F1D0B), blurRadius: 20, offset: Offset(0, 8)),
  ];

  static const floating = [
    BoxShadow(color: Color(0x1A5B8F27), blurRadius: 24, offset: Offset(0, 10)),
  ];
}
